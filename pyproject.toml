[project]
name = "review-server"
version = "0.2.1"
requires-python = ">=3.10"
dependencies = ["hcloud>=2.4", "requests>=2.28"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[dependency-groups]
dev = ["pytest"]
